# Liberu Boilerplate Scripts

Release automation for the Liberu modular Laravel ecosystem.

- `publish-components` creates and updates independent component repositories while preserving their histories.
- `submit-packagist.php` registers the root project, modules, themes, and scripts package with Packagist.

```bash
composer require --dev liberusoftware/boilerplate-scripts
```
