name: Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  tests:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - uses: actions/checkout@v4

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.5'
<<<<<<< HEAD
          extensions: mbstring, pdo, pdo_sqlite, bcmath, opcache, zip, gd, intl
=======
          extensions: mbstring, bcmath, pdo, pdo_sqlite, sqlite3, dom, curl, intl, gd, redis
>>>>>>> c424b4450cca672d73cd247d8c0aa549371c00b5
          coverage: xdebug

      - name: Get composer cache directory
        id: composer-cache
        run: echo "dir=$(composer config cache-files-dir)" >> $GITHUB_OUTPUT

      - name: Cache composer dependencies
        uses: actions/cache@v4
        with:
          path: ${{ steps.composer-cache.outputs.dir }}
          key: ${{ runner.os }}-composer-${{ hashFiles('**/composer.lock') }}
<<<<<<< HEAD
          restore-keys: ${{ runner.os }}-composer-
=======
          restore-keys: |
            ${{ runner.os }}-composer-

      - name: Install dependencies
        run: composer install --no-progress --prefer-dist --no-interaction
>>>>>>> c424b4450cca672d73cd247d8c0aa549371c00b5

      - name: Copy environment file
        run: cp .env.example .env

<<<<<<< HEAD
      - name: Install dependencies
        run: composer install --no-interaction --prefer-dist --optimize-autoloader

      - name: Generate application key
        run: php artisan key:generate

      - name: Run tests
        run: php artisan test --coverage-clover=coverage.xml
=======
      - name: Generate application key
        run: php artisan key:generate

      - name: Run tests with SQLite in-memory
        run: vendor/bin/pest --coverage --coverage-clover=coverage.xml
        env:
          DB_CONNECTION: sqlite
          DB_DATABASE: ':memory:'
>>>>>>> c424b4450cca672d73cd247d8c0aa549371c00b5

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v5
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: ./coverage.xml
          flags: unittests
          name: codecov-umbrella
          fail_ci_if_error: false
