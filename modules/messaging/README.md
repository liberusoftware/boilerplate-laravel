# Liberu Messaging

Owns private message persistence, participant relationships, unread state, encryption-at-rest expectations, and server-side policy. Identity resolution uses the configured authentication model. API/UI adapters are optional and domain transactions never depend on them.
