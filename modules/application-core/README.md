# Liberu Application Core

Required host-neutral application primitives and secure HTTP defaults. It validates deployment invariants, exposes `/ready`, provides an extensible readiness registry, UTC clock and UUID contracts, release metadata, maintenance policy, and security headers. The host retains environment composition and the liveness route.
