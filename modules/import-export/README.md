# Liberu Import and Export

Owns versioned transfer schemas, field mapping, dry runs, validation, authorized queued execution, progress, expiring artifacts, and row-level error reports. Product modules register schemas rather than exposing internal tables.
