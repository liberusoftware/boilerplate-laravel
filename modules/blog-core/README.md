# Liberu Blog Core

Owns blog posts, publishing persistence, public routes, and functional views. It has no Filament dependency. Set `blog.author_model` to the host identity model implementing the required author relationship.
