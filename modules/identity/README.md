# Liberu Identity

Defines provider-neutral identity registration policy, canonical identifier normalization, recent-auth configuration, and versioned security-event envelopes. UI and framework-specific actions live in adapters such as Jetstream Bridge.
