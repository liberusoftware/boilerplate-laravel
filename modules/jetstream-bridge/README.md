# Liberu Jetstream Bridge

Adapts Liberu identity/profile/team actions to Fortify and Jetstream contracts without making either framework package the domain layer. It uses the host's configured authentication model, strong password defaults, identity/network rate limits, and transactional account deletion. Domain modules consume identity contracts rather than these adapter classes.
