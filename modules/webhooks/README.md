# Liberu Webhooks

Owns filtered endpoint registrations, timestamped HMAC signing, secret rotation, durable delivery attempts, exponential backoff, idempotent event delivery, replay, and redacted diagnostics.
