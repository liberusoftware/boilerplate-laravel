# Liberu Identity Filament

Optional Filament 5 administration adapter. It resolves the host authentication model from configuration and does not import `App` classes. Applications compose its plugin into an authorized staff panel.
