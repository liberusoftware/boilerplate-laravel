# Liberu Integrations

Owns the provider-adapter registry and protected connection lifecycle: scoped encrypted credentials, capability discovery, OAuth boundaries, rotation, connection tests, sync status, and reconciliation evidence.
