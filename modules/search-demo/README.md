# Liberu Search Demo

Owns the reference post and group records used by the boilerplate to demonstrate and integration-test the provider-neutral Search module. Product applications should replace its configured models or omit this package.
