# Liberu Messaging API

Optional authenticated API adapter for private messaging. It validates inputs, enforces participant policies, prevents self-messaging, and exposes only required identity fields.
