# Liberu Localization

Resolves a permitted locale from request, session, authenticated actor, browser negotiation, and application fallback. Includes the `language-switcher` Livewire component and module view. Hosts attach `SetLocale` at their trusted HTTP boundary and provide translated domain resources in owning modules.
