<?php

return [
    'models' => [
        // Defaulted to the application's auth provider model by the service
        // provider. A package owning another searchable concept adds its own key
        // here and registers a searcher for it.
        'user' => null,
    ],
];
