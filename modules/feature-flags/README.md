# Liberu Feature Flags

Owns typed flags, deterministic percentage rollout, environment/tenant/actor targeting, ownership, expiry, and evaluation telemetry boundaries. Flags never replace module installation, enablement, entitlement, or authorization.
